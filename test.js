//Test

//Scaling to unit length
/*console.log(value(
    myPerceptron.activate(
        [value(3.254, input, 0, norm),
         value(1368, input, 1, norm)
        ]),
        output,
        null,
        real)
); //0*/

//Rescaling
console.log(value(
    myPerceptron.activate(
        [value(3.254, input, 0, norm),
         value(1368, input, 1, norm)
        ]),
        output,
        null,
        real)
); //0
